import React from "react";
import { Navigate, Header } from "../components";
import styled from "styled-components";

const Profile = () => {
  return <div>It works!</div>;
};

export default Profile;
